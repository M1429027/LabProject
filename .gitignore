# =========== 第一步：鎖國政策 ===========
# 忽略所有檔案 (不管你是 json, mp4, jpg 還是什麼，通通擋掉)
*

# =========== 第二步：開門讓 Git 進入資料夾 ===========
# 如果不加這行，Git 連資料夾都不會進去，就找不到深層的 .py 檔了
!*/

# =========== 第三步：排除地雷區 (非常重要！) ===========
# 雖然我們只收 .py，但虛擬環境 (venv) 裡面有幾萬個 .py 檔是函式庫的，
# 我們不想上傳那些東西！請把你有的虛擬環境名稱寫在這
.venv/
venv/
env/
__pycache__/
.git/
.idea/
.vscode/

# =========== 第四步：發放通行證 (白名單) ===========
# 只有 .py 檔案可以通過
!*.py

# =========== 新增的黑名單 ===========
# 雖然他是 py 檔，但我不想讓他進去
# (請把不想上傳的檔名列在下面，一行一個)
visualization.py
triangutest.py
media.py
mediavideo.py
jointstest.py
test.py
yolo.py
notebooks/
src/
psbody-mesh/

# 不要忘了讓 .gitignore 自己也能通過，不然下次它就不見了
!.gitignore

# (選填) 建議這兩個也留著，不然專案會看起來像孤兒
!README.md
!requirements.txt