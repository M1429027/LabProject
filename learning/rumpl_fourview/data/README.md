# `learning/rumpl_fourview/data`

This directory stores non-code reference assets for the four-view transformer:

- `sample_manifest.json`: example dataset manifest structure
- `base_rig.schema.json`: example schema for a four-camera base rig used by the synthetic generator

Large synthetic datasets and checkpoints should be written under `outputs/`, not committed here.
