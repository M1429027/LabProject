# Data Notes

This directory stores lightweight metadata for the `karate_selfcal` project.

It should contain:

- schemas for intermediate JSON files
- small manifests
- example metadata

It should not contain:

- raw videos
- large extracted frames
- model checkpoints
- tracked experiment outputs
